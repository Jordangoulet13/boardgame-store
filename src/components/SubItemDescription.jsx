import styled from "styled-components";

const SubItemDescription = ({ item }) => {
  return <Container></Container>;
};

export default SubItemDescription;

const Container = styled.div`
  width: 100%;
  height: 30rem;
  background-color: #fff;
`;
